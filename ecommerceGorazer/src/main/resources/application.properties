spring.application.name=ecommerceGorazer
server.port=${SERVER_PORT:8080}

# Database Configuration
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-rough-cherry-a5b8gsg2-pooler.us-east-2.aws.neon.tech/gorazerdb?sslmode=require&channelBinding=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:gorazerdb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:tLXnVoPKUc57}

# JPA / Hibernate
spring.jpa.database=POSTGRESQL
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Mail Configuration
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:tu-email@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:tu-password-app}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Secret
app.jwt.secret=${APP_JWT_SECRET:MySecretKeyThatShouldBeVeryLongAndSecure1234567890}
app.jwt.expiration-ms=86400000

# Stripe
stripe.api.key=${STRIPE_API_KEY:sk_test_placeholder_key}

# NewsAPI
newsapi.key=${NEWSAPI_KEY:4aac32271a424df4b67ae71207b2cc48}

# Cache (Caffeine)
spring.cache.type=caffeine
spring.cache.cache-names=techNews
spring.cache.caffeine.spec=maximumSize=100,expireAfterWrite=30m